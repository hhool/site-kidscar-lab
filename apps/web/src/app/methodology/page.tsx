import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function Page() {
  return (
    <PlaceholderPage
      title={{ zh: "测试方法", en: "Methodology" }}
      description={{ zh: "指标定义、评分规则和版本说明。", en: "Metric definitions, scoring rules, and version notes." }}
    />
  );
}
